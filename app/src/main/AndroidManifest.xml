<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.utci.app4p">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App4p">
        <activity android:name=".GenerarRolActovity"></activity>
        <activity android:name=".RolActivity" />
        <activity android:name=".CrudEmpleado" />
        <activity android:name=".EditarEmpleadoActivity" />
        <activity android:name=".RegistroEmpleadosActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".BienvenidaActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>